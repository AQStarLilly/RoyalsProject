using UnityEngine;

public class ComputerPlayers : MonoBehaviour
{
    /* Computer Needs ---
     * Track teammates and opposing players
     * Track the puck and goals (theirs and opposing net)
     * Track who has puck if being held 
     * (Ally team - move forwards) (Opposing Team - move backwards to position between net and player)
     * 
     * 
     * Needs Movement
     * Shooting and Passing
     * Defense ? (not sure how it'll work yet)
     * 
     * 
     * 
     */

    // Add a way for players to steal puck if near player holding it?

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
